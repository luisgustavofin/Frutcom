import React from 'react';
import { Text } from 'react-native';
import { TextStyles } from '../../utils';

export const PrincipalText = ({ txt }) => <Text style={TextStyles.h1BlackBold}>{txt}</Text>;
